方式一：程序可以直接运行，运行该程序之前，需要将当前目录置为本程序所在目录，程序会读取当前目录下的npc_nts_client_config.ini配置文件，读取固定配置参数后，再读取该配置文件中的参数CfgFileName指向的文件，该配置文件主要保存些可以被修改的参数。

方式二：程序后指定配置参数文件名（包括路径），格式为-c 文件名及路径 后续可以携带UMID，程序首先读取该文件，然后再读取参数CfgFileName指向的文件，该参数如果配置为#号。如果UMID没有携带，则不处理，按配置文件的UMID来处理。表达式：
ntsclient -c [CONFIG_PATH] [UMID]